import React from 'react';

const DummyText = ({text}) => {
    return (
        <div>
            <h1 className='bg-black text-5xl text-white'>{text}</h1>
        </div>
    );
};

export default DummyText;